// Shared input styling so every admin field looks identical.
export const inputClass =
  "w-full rounded-lg border border-border bg-white px-4 py-3 text-text outline-none transition-colors placeholder:text-muted focus:border-accent";

export const textareaClass = `${inputClass} resize-y font-mono text-sm`;
